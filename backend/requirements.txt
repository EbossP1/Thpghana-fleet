# ═══════════════════════════════════════════════════════════════
# REQUIREMENTS.TXT UPDATE
# ═══════════════════════════════════════════════════════════════

psycopg[binary]==3.2.1
psycopg-pool==3.2.2
fastapi
uvicorn
python-jose[cryptography]
bcrypt==4.0.1
python-dotenv
python-multipart
PyJWT

# The key new dependency is psycopg-pool which provides ConnectionPool.
# It's a separate package from psycopg itself.
